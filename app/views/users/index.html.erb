<% provide(:title, 'All users') %>
<h1>All users</h1>
<%= will_paginate %>
<ul class="users">
<!-- Rails automatically iterates through @users object by rendering @users -->
    <%= render @users %>

</ul>
<%= will_paginate %>